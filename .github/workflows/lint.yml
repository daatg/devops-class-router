name: lint
on: 
  pull_request:
    types: [opened, reopened]
env:
  RUSTFLAGS: "-Dwarnings"
jobs:
  clippy-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run Clippy
        run: |
          ls
